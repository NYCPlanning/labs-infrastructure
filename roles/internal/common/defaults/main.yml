# real people
users:
  - allthesignals
  - andycochran
  - trbmcginnis
  - hannahkates
  - godfreyyeung
  - pichot
former_users:
  - afeld
  - chriswhong
  - julialucyhogan
machine_users:
  - deployer
  - dokku
ssh_users: "{{ users + machine_users }}"
ssh_allow_users: "{{ ssh_users|join(' ') }}"
