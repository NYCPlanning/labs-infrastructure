# real people
users:
  - allthesignals
  - andycochran
  - trbmcginnis
  - hannahkates
  - julialucyhogan
former_users:
  - afeld
  - chriswhong
machine_users:
  - deployer
  - dokku
ssh_users: "{{ users + machine_users }}"
ssh_allow_users: "{{ ssh_users|join(' ') }}"
